import styled from 'styled-components';

const NodeRule = styled.h3`
  margin: -0.5rem 0 1rem;
  padding: 0 5px;
  font-size: 0.833rem;
  font-weight: normal;
  opacity: 0.5;
  text-transform: capitalize;
`;

export default NodeRule;
