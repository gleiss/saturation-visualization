import styled from 'styled-components';

const Card = styled.section`
  margin: 0 -0.5rem;
  background: #fff;
  border-radius: 2px;
  box-shadow: 1px 3px 10px 0 rgba(0, 0, 0, .1);
`;

export default Card;
